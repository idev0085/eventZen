import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SessionExpiredModal = () => {
  return (
    <View>
      <Text>SessionExpiredModal</Text>
    </View>
  );
};

export default SessionExpiredModal;

const styles = StyleSheet.create({});
